<!--pages/callOrder/callOrder.wxml-->
<view class="container">
  <view class="call-header">
    <view class="header-title">
      <view class="title-name">APP群呼</view>
      <view class="check">
        <view class="section section_gap">
          <view class="body-view">
            <switch checked="{{switchChecked}}" bindchange="switch1Change" />
          </view>
        </view>
      </view>
    </view>
    <view class="call-tips">
      <text>开启后对讲门禁呼叫手机APP讲同时呼叫所有住户，呼叫手机电话仍然按顺序呼叫。</text>
    </view>
  </view>
  <view class="order-title-name">
    <text>呼叫顺序设置</text>
  </view>
  <view bindtap="box" class="box">
    <view disable-scroll="true" wx:for="{{list}}" wx:key="*this" bindtouchmove="move" bindtouchstart="movestart" bindtouchend="moveend" data-index="{{item.id}}" data-main="{{mainx}}" class="list-order main {{mainx == item.id? 'mainmove':'mainend'}}" style="left:{{start.x}}px; top:{{start.y}}px">
      <view class="item-content">
        <view class="number">
          <text>{{item.id}}</text>
        </view>
        <view class="item-face">
          <image src="{{item.faceUrl}}"></image>
        </view>
        <view class="item-name">
          <text>{{item.name}}</text>
        </view>
      </view>
      <view class="order-icon">
        <image src="../images/order_icon.png"></image>
      </view>
    </view>
  </view>
</view>